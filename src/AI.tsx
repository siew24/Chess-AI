import React from "react";

// A file which how the AI should interact with the game

// This function should return a 2D array
export function doSomethingHere(board: Array<Array<string>>): Array<Array<string>> {
    return [[]];
}